name: CI/CD
on:
  push:
    branches: ['main']
jobs:
  checkout:
    name: Checkout
    runs-on: ubuntu-20.04

    steps:
      - name: GGGG
        uses: actions/checkout@v3
        uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
        run: npm ci
        run: ls -al
