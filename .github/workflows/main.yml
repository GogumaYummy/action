name: CI/CD
on:
  push:
    branches: ['main']
jobs:
  checkout:
    name: Hi
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
        run: npm ci
        run: ls -al
